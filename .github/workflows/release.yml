name: Hello Test Action

on:
  pull_request:   # 当有人提 PR 时运行
  workflow_dispatch:   # 手动触发也行

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "✅ Hello from GitHub Action! 触发成功"
